DB_HOST=localhost
DB_PORT=5432
DB_USER=bluer_book
DB_PASS=xxx
DB_NAME=bluer_book

# The non-owner role the server connects as, so isolation applies to it.
# `migrate` creates it; `server` refuses to start without APP_DB_USER set.
APP_DB_USER=bluer_book_app
APP_DB_PASS=xxx
