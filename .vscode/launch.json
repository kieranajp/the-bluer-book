{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Import",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "args": ["import"],
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
