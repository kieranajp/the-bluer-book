import 'package:flutter/material.dart';

/// App color palette
class Colours {
  // Backgrounds
  static const background = Color(0xFFF6F7F7);
  static const surface = Colors.white;

  // Primary colors
  static const primary = Color(0xFF4E6983);

  // Text colors
  static const textPrimary = Color(0xFF121416);
  static const textSecondary = Color(0xFF67737E);

  // Borders
  static const border = Color(0xFFF0F0F0);

  // Shadows
  static final shadowLight = Colors.black.withOpacity(0.05);
}
