{{- if .Values.ingress.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  entryPoints:
    - websecure
  routes:
    # MCP route — no auth (MCP server handles its own auth)
    - kind: Rule
      match: Host(`{{ .Values.ingress.host }}`) && PathPrefix(`/mcp`)
      services:
        - name: {{ .Release.Name }}-mcp
          port: {{ .Values.mcp.service.port }}
    # API routes — JWT bearer auth (for mobile/programmatic clients)
    - kind: Rule
      match: Host(`{{ .Values.ingress.host }}`) && PathPrefix(`/api`)
      middlewares:
        - name: jwt-auth
          namespace: auth
      services:
        - name: {{ .Release.Name }}
          port: {{ .Values.app.service.port }}
    # Everything else — browser session auth (Google login via Kratos)
    - kind: Rule
      match: Host(`{{ .Values.ingress.host }}`)
      {{- if .Values.ingress.middlewares }}
      middlewares:
        {{- range .Values.ingress.middlewares }}
        - name: {{ .name }}
          namespace: {{ .namespace }}
        {{- end }}
      {{- end }}
      services:
        - name: {{ .Release.Name }}
          port: {{ .Values.app.service.port }}
  tls:
    certResolver: letsencrypt
{{- end }}
